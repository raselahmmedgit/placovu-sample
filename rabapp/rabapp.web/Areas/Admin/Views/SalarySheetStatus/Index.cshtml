@model IEnumerable<rabapp.web.Models.SalarySheetStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SalarySheetStatusName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SalarySheetStatusName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalarySheetStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalarySheetStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalarySheetStatusId })
        </td>
    </tr>
}

</table>
