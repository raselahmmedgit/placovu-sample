@model IEnumerable<rabapp.web.Models.EmployeeClassType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeClassTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeClassTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmployeeClassTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmployeeClassTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmployeeClassTypeId })
        </td>
    </tr>
}

</table>
