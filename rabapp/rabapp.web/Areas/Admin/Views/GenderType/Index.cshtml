@model IEnumerable<rabapp.web.Models.GenderType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GenderTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GenderTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GenderTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GenderTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GenderTypeId })
        </td>
    </tr>
}

</table>
