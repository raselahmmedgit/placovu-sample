@model IEnumerable<rabapp.web.Models.SalaryHeadType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SalaryHeadTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SalaryHeadTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalaryHeadTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalaryHeadTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalaryHeadTypeId })
        </td>
    </tr>
}

</table>
