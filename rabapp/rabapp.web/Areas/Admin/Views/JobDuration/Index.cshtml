@model IEnumerable<rabapp.web.Models.JobDuration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobDurationName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobDurationYear)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JobDurationName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JobDurationYear)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobDurationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobDurationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobDurationId })
        </td>
    </tr>
}

</table>
