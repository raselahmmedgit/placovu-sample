@model IEnumerable<rabapp.web.Models.ResidentType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ResidentTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ResidentTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ResidentTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ResidentTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ResidentTypeId })
        </td>
    </tr>
}

</table>
