@model IEnumerable<rabapp.web.Models.FreedomFighterRelationshipType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FreedomFighterRelationshipTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FreedomFighterRelationshipTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FreedomFighterRelationshipTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FreedomFighterRelationshipTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FreedomFighterRelationshipTypeId })
        </td>
    </tr>
}

</table>
