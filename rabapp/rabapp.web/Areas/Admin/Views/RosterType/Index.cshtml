@model IEnumerable<rabapp.web.Models.RosterType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RosterTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RosterTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RosterTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RosterTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RosterTypeId })
        </td>
    </tr>
}

</table>
