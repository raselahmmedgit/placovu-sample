@model IEnumerable<rabapp.web.Models.EmployeeCurriculamActivitieInfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.EmployeeCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.EmployeeCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmployeeCurriculamActivitieInfoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmployeeCurriculamActivitieInfoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmployeeCurriculamActivitieInfoId })
        </td>
    </tr>
}

</table>
