@model IEnumerable<lab.SecurityApp.Models.Widget>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WidgetCategory.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsAdmin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsShared)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WidgetCategory.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsAdmin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsShared)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WidgetId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WidgetId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WidgetId })
        </td>
    </tr>
}

</table>
