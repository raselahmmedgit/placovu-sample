@model IEnumerable<lab.SecurityApp.Models.MenuRolePermission>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Menu.MenuName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Role.RoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Menu.MenuName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Role.RoleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MenuRolePermissionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MenuRolePermissionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MenuRolePermissionId })
        </td>
    </tr>
}

</table>
